{% extends 'base.html.twig' %}

{%  block metacharset %}
    <meta charset="UTF-8" />
{% endblock %}

{% block pageTitle %}Organismes{% endblock %}

{% block body %}
<h2>Liste des organismes</h2>
<div class="container">
    <div class="row">
        <div class="col-xs-12">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">
                <i class="fa fa-plus-square fa-lg" aria-hidden="true"></i> Ajouter
            </button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
<table class="table table-striped">
    <thead>
        <th>Id</th>
        <th>Nom de l'organisme</th>
    </thead>
    <tbody>
    {% for organisme in listeOrganismes %}
        <tr>
            <td>{{ organisme.idOrganisme }}</td>
            <td>{{ organisme }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
